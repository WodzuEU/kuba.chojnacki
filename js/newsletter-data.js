/* ============================================================
   NEWSLETTER ARCHIVE — past issues, newest first.

   Each time you publish a broadcast in Kit, add one line here.
   The /newsletter page renders this list in the site's own type;
   subscribers come in through Kit, the archive lives on your domain.

   Fields:
     date    ISO date the issue was sent — "2026-07-10"
     title   the issue's subject line / title
     url      public web version of the issue (Kit → Broadcast →
              "View in browser" / public URL). Optional; if omitted
              the entry shows without a "read" link.
     excerpt  one short lowercase line of preview text. Optional.

   Leave the array empty until the first issue goes out — the page
   shows a graceful "first issue on its way" state on its own.

   (This file can also be regenerated automatically from Kit's RSS
   feed by a GitHub Action — see README → Newsletter.)
   ============================================================ */

const NEWSLETTER_ISSUES = [
  // { date: "2026-07-10", title: "first light", excerpt: "new atmosphere works, and a note on gravity.", url: "https://…" },
];
